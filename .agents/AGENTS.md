# Git Rules
Always run `git add .`, `git commit`, and `git push` after making any functional change or completing a request, no matter how small. The user requires that every minimum change must be uploaded to the repo immediately.
